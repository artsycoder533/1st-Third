import React, { useReducer } from "react";



const reducer = (state, action) = {
    switch(action) {
        // case 'OPEN MENU':
        //     return state;
        // case 'CLOSE_MENU':
        //     return state;
        // default:
        //     return state;
    }
}